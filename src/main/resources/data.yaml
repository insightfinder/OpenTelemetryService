users:
  maoyuwang:
    licenseKey: ""
    log:
      extraction:
        projectFrom:
          - source: "static"
            value: "maoyu-test-otlpserver"

        systemFrom:
          - source: "static"
            value: "maoyu-test-otlpserver"

        instanceFrom:
          - source: "body"
            field: "_source.nifi_node"
            regex: ".*"

        componentFrom:
          - source: "body"
            field: "_source.eventType"
            regex: ".*"

        timestampFrom:
          - source: "body"
            field: "_source.timestamp"
