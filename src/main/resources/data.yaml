users:
  maoyuwang:
    licenseKey: "123"
    log:
      extraction:
        projectFrom:
          - source: "static"
            value: "maoyu-e2e-test-otlp-log-2"
        systemFrom:
          - source: "static"
            value: "maoyu-e2e-test-otlpserver"
        instanceFrom:
          - source: "body"
            field: "_source.node"
            regex: ".*"
        componentFrom:
          - source: "body"
            field: "_source.eventType"
            regex: ".*"
        timestampFrom:
          - source: "body"
            field: "_source.timestamp"
    trace:
      extraction:
        projectFrom:
          - source: "static"
            value: "maoyu-e2e-test-otlp-trace-2"
        systemFrom:
          - source: "static"
            value: "maoyu-e2e-test-otlpserver"
        instanceFrom:
          - source: "body"
            field: "spanAttributes.net.host.name"
            regex: ".*"
        componentFrom:
          - source: "body"
            field: "traceAttributes.service.name"
            regex: ".*"
